# SRE Bot Demo Dependencies

# Core HTTP client for Prometheus queries
httpx>=0.25.0

# Environment variable management
python-dotenv>=1.0.0

# Claude Agent SDK (includes MCP support)
# Note: Also requires Claude Code CLI: npm install -g @anthropic-ai/claude-code
claude-agent-sdk>=0.1.0

# Slack integration
slack-bolt>=1.18.0
aiohttp>=3.9.0
